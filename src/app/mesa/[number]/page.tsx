import {notFound} from "next/navigation";import {TableEntry} from "@/components/cliente/table-entry";
export default async function Page({params,searchParams}:{params:Promise<{number:string}>;searchParams:Promise<{token?:string;encerrado?:string}>}){const [{number},{token="",encerrado}]=await Promise.all([params,searchParams]);const parsed=Number(number);if(!Number.isInteger(parsed)||parsed<1||parsed>45||!token)return notFound();return <TableEntry tableNumber={parsed} accessToken={token} forceClosed={encerrado==="1"}/>}
