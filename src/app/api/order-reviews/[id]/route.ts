import {NextRequest,NextResponse} from "next/server";import {apiErrorResponse} from "@/lib/api/errors";import {orderReviewSchema} from "@/lib/api/schemas";import {createAdminSupabaseClient} from "@/lib/supabase/server";
export async function POST(request:NextRequest,context:{params:Promise<{id:string}>}){try{const {id}=await context.params,input=orderReviewSchema.parse(await request.json());const {data,error}=await createAdminSupabaseClient().rpc("create_order_review",{requested_order_id:Number(id),requested_tracking_token:input.trackingToken,requested_rating:input.rating,requested_comment:input.comment});if(error)throw error;return NextResponse.json({id:data.id,rating:data.rating,comment:data.comment,createdAt:data.created_at},{status:201})}catch(error){return apiErrorResponse(error)}}
