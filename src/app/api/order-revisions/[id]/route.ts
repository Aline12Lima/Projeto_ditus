import {NextRequest,NextResponse} from "next/server";import {requireStaff} from "@/lib/api/auth";import {apiErrorResponse} from "@/lib/api/errors";import {reviseOrderSchema} from "@/lib/api/schemas";import {reviseOrder,getOrder} from "@/server/services/orders";
export async function PUT(request:NextRequest,context:{params:Promise<{id:string}>}){try{const {admin}=await requireStaff();const {id}=await context.params,input=reviseOrderSchema.parse(await request.json());await reviseOrder(admin,Number(id),input);return NextResponse.json(await getOrder(admin,Number(id)))}catch(error){return apiErrorResponse(error)}}
