import { NextRequest,NextResponse } from "next/server";import { requireAdmin } from "@/lib/api/auth";import { apiErrorResponse } from "@/lib/api/errors";import { assignCustomerVisitSchema } from "@/lib/api/schemas";import { assignCustomerVisit } from "@/server/services/customer-visits";
export async function PATCH(request:NextRequest,context:{params:Promise<{id:string}>}){try{const {admin}=await requireAdmin();const {id}=await context.params;const {tableNumber}=assignCustomerVisitSchema.parse(await request.json());return NextResponse.json(await assignCustomerVisit(admin,id,tableNumber))}catch(error){return apiErrorResponse(error)}}
